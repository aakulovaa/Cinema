@model IEnumerable<Cinema.Models.TicketSold>

@{
    ViewBag.Title = "Index";
}

<h2>TicketSold</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.Session.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeopleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlaceNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CashRegister.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Session.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PeopleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlaceNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CashRegister.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
